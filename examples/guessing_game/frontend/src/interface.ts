export interface Guess {
  guess: string;
} /** derive:Clone; **/

export interface Answer {
  answer: string;
} /** derive:Clone; **/

export interface Interactions {
  close: boolean;
} /** derive:Clone; **/
